import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wlsib.app',
  appName: 'ﾚﾝｽﾞ何持ってく',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
